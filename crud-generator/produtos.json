{
    "fields": [
        {
            "name": "nome",
            "type": "string"
        },
        {
            "name": "is_bolo_extra",
            "type": "select",
            "options": {
                "s": "Sim",
                "n": "Não"
            }
        }
    ],
    "validations": [
        {
            "field": "nome",
            "rules": "required|string|max:255"
        },
        {
            "field": "is_bolo_extra",
            "rules": "required"
        }
    ]
}
